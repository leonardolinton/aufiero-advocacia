function handleSubmit(event) {
    event.preventDefault(); // Impede o comportamento padrão (recarregar a página)
    // Aqui você pode adicionar o código que deseja executar ao enviar o formulário
    alert('Formulário em fase de Teste!');
}